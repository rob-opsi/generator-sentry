import React from 'react';
import PropTypes from 'prop-types';

class <%= name %> extends React.Component {
  static propTypes = {
  };

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
      </div>
    );
  }
}

export default <%= name %>;
