import React from 'react';
import PropTypes from 'prop-types';

class <%= name %> extends React.PureComponent {
  static propTypes = {
  };

  render() {
    return (
      <div>
      </div>
    );
  }
}

export default <%= name %>;
